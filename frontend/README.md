# FlashReserve frontend

React + Vite (JavaScript) client for FlashReserve.

```bash
npm install
npm run dev      # start the dev server
npm run build    # production build
npm run lint     # oxlint
```

Backend API base URL is configured via `frontend/.env` (see `.env.example`).
See the repository root `README.md` for the full project documentation.